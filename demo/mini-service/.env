# Public fixture only. Every value is intentionally fake and must be excluded by the selector.
SERVICE_NAME=mini-service
SERVICE_PORT=8080
DEMO_API_KEY=fake-demo-value-not-a-real-secret
DEMO_TOKEN=fake-demo-token-not-a-real-secret
