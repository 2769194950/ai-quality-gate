# node_modules placeholder (demo fixture)

This file exists only so that the deterministic selector can prove
`node_modules/**` is excluded by the built-in default excluded path rule, even
when the user include pattern is `**/*`. It carries no dependency and nothing
imports it.
