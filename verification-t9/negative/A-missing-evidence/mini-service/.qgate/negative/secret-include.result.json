{
  "schemaVersion": "1.0",
  "caseId": "secret-include",
  "purpose": "Counter-example: even the widest include pattern (\"**/*\") must not re-admit a secret path, because the secret-path filter runs before user include (REQ-QUALITY-GATE-012).",
  "command": "node packages/qgate/bin/qgate.mjs preview --root demo/mini-service --config demo/qgate.config.json --json",
  "exitCode": 0,
  "assertion": {
    "expression": "selection.included.filter(p => /(^|\\/)(\\.env|.*\\.pem|.*\\.key|id_rsa.*|credentials.*|secrets\\/)/.test(p)).length === 0",
    "expected": 0,
    "actual": 0
  },
  "excludedExamples": [
    { "path": ".env", "reason": "secret_path", "rule": "secret-env" },
    { "path": "secrets/payments.pem", "reason": "secret_path", "rule": "secret-pem" },
    { "path": "config/service.key", "reason": "secret_path", "rule": "secret-key" }
  ],
  "recorded_at": "2026-05-05T10:00:00Z"
}
